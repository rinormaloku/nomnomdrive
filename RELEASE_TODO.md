- Add to desktop UI the option to login and connect to the cloud instance (for now, we can just link to the CLI command). This would be an alternative to
```
node packages/desktop/dist/cli/index.js cloud login --server http://localhost:3030
```
- Add chat to the CLI
- How to run the CLI as a deamon
- NPX straight to linux/macos/windows service?